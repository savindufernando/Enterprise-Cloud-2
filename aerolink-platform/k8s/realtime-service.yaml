apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-service
  namespace: aerolink
  labels:
    app: realtime-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: realtime-service
  template:
    metadata:
      labels:
        app: realtime-service
    spec:
      containers:
      - name: realtime-service
        image: ghcr.io/savindufernando/enterprise-cloud-2-realtime_service:latest
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
        ports:
        - containerPort: 8007
        envFrom:
        - configMapRef:
            name: aerolink-config
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8007
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8007
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: realtime-service
  namespace: aerolink
spec:
  selector:
    app: realtime-service
  ports:
    - protocol: TCP
      port: 8007
      targetPort: 8007
      name: http
